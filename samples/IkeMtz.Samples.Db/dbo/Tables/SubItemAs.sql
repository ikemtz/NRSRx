CREATE TABLE [dbo].[SubItemAs]
(
    [Id] UNIQUEIDENTIFIER NOT NULL,
    [ValueA] VARCHAR (50) NOT NULL,
    [ItemId] UNIQUEIDENTIFIER NOT NULL,
    CONSTRAINT [PK_SubItemAs] PRIMARY KEY CLUSTERED ([Id] ASC),
    CONSTRAINT [FK_SubItemAs_Items] FOREIGN KEY ([ItemId]) REFERENCES [dbo].[Items] ([Id])
);


